[pytest]
testpaths = tests

# strict mode: async tests must carry an explicit @pytest.mark.asyncio, which the
# existing suite already does, and async fixtures use @pytest_asyncio.fixture.
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
